---
description: Set up Git for a project -- runs git-setup agent
---
Set up Git for this project using the `git-setup` agent.

Collect: owner name, email, GitHub username, org, remote URL(s), branch name, environment.
Then configure identity, wire remotes, make initial commits, push, write git-config.md.

$ARGUMENTS
