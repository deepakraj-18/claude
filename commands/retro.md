---
description: Review quality log and extract patterns -- runs quality-logger in retrospective mode
---
Run a quality retrospective for this project using `quality-logger` agent in phase-retrospective mode.

Analyse all failures in `.claude-context/quality-log.md`, identify recurring patterns,
update `~/.claude/retrospective/patterns.md`, and recommend specific rule/agent changes.

$ARGUMENTS
